package framework.frameworkScript;

import framework.environment.FrameworkScript;
import framework.environment.Scene;

public class AddObjects implements FrameworkScript
{
    @Override
    public synchronized void run(Scene scene)
    {

    }
}
